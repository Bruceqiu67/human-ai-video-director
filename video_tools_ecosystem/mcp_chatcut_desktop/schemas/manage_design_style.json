{"name":"manage_design_style","description":"Manage the user's library of Design Styles. A design style is the ONLY representation of brand identity in this product — the currently applied design style IS the project's design style. There is no separate \"project-only\" brand.\n\nActions split into two namespaces by storage:\n\n# User library actions (DB-backed)\n- list: show the current project's active design style, then all of the user's saved design styles.\n- get: show the full designSpec for the current project's active design style. Pass designStyleId to inspect a specific saved design style or catalog preset. A project with no design style is a normal result, not an error: the response says so and states how to route (save a known brand with `create`, or offer presets when there is no brand).\n- create: make a new design style from a designSpec. **Auto-applies to the current project by default** — pass `applyToProject: false` if you only want to add it to the library without switching this project to it. Pass a clear name (\"Acme\", \"{Client} · Winter campaign\") — avoid generic names. **DO NOT use `create` to apply a catalog preset — use `apply_preset` instead, which avoids retyping the spec.**\n- update: merge a patch into an existing design style's spec, rename it, and/or set its `thumbnailUrl` cover. By default targets the design style currently applied to this project; pass designStyleId to target a specific one. If the design style is used by \u003e1 project, the tool returns a confirmation_required response (see below) — you MUST ask the user and, if they want to proceed, re-call with `confirmShared: true`. If they'd rather have a separate variant without affecting other projects, use the `create` action to make a new design style instead.\n- apply: switch the current project to a different existing design style. Requires designStyleId.\n- clear: remove the active design style from the current project without deleting saved user styles or catalog presets. Use this before validation runs that need to test first-run style alignment.\n- delete: permanently remove a design style. Destructive — the tool returns confirmation_required on first call (see below); you MUST confirm with the user before re-calling with `confirm: true`. Deletion does NOT remove assets already used in existing projects — it just removes the design style from the library, so it can no longer be re-applied.\n\n# Catalog actions (file-backed curated presets, read-only)\n- list_presets: enumerate the curated catalog of design style presets (rows in design_style where ownerId IS NULL AND visibleInFrontend). Returns id (UUID) / name / thumbnail URL / applicable scenarios / description per preset — enough for host-specific widget-forms skills to render a visual picker, or for hosts without visual cards to present numbered choices. Does NOT return the persisted designSpec. Optional `scenario` arg filters by applicable scenario tag for recommendation / selection. The returned `description` is the main agent-facing style profile for matching presets to the user's requested visual direction. Optional `locale` arg returns localized name/description (pass the user's chat language: 'en' / 'zh' / 'zh-hant' / 'es' / 'ja'; defaults to English). This is the single source of truth for \"what styles can the user pick\" — never hardcode an option list.\n- apply_preset: pin the current project's active design style directly to a curated catalog preset. Pass only `presetId` (the UUID returned by list_presets). No clone is created — the project's `designStyleId` FK points at the catalog row itself. A user-owned copy is only forked later if the user actually edits the spec (handled in `update`). This is the correct path after the user picks a catalog design style from the picker. For MG/design work, call `get` after applying; `list_presets` names/descriptions are picker summaries, not the full designSpec.\n\n# Confirmation protocol (update shared / delete)\n\nWhen the tool needs user confirmation, the response starts with `status: \"confirmation_required\"` and states the impact plus the exact re-call flag (`confirmShared: true` for a shared-style update, `confirm: true` for delete). When you see it, STOP and ask the user. Don't silently re-call; don't silently proceed. Relay the situation faithfully and let the user decide.\n\n# designSpec shape\n\n`thumbnailUrl` is a Design Style UI cover for library / picker display only. It is not a generation reference. Use it when the user asks to set the Design Style cover, or when importing a style page with a clear preview image. Do not duplicate that same image in `images[]` unless it should also influence generation.\n\nEvery element has exactly one label field — `role` — that says **what it's for / how it's used**. Not what it looks like.\n\n```json\n{\n  \"colors\": [\n    { \"role\": \"primary\",    \"value\": \"#6C5CE7\" },\n    { \"role\": \"accent\",     \"value\": \"#A8E6CF\" },\n    { \"role\": \"background\", \"value\": \"#FCFBF8\" },\n    { \"role\": \"CTA button\", \"value\": \"#FF6B35\" }\n  ],\n  \"fonts\": [\n    { \"role\": \"heading\", \"family\": \"Inter\" },\n    { \"role\": \"body\",    \"family\": \"Inter\" }\n  ],\n  \"logos\": [\n    { \"role\": \"wordmark\",       \"url\": \"https://...\" },\n    { \"role\": \"icon\",           \"url\": \"https://...\" },\n    { \"role\": \"dark-bg variant\",\"url\": \"https://...\" }\n  ],\n  \"images\": [\n    { \"role\": \"product hero\",  \"url\": \"https://...\" },\n    { \"role\": \"team photo\",    \"url\": \"https://...\" }\n  ],\n  \"styleGuide\": \"Modern, technical, confident.\"\n}\n```\n\nField rules:\n- **colors[]**: `value` hex (required). `role` says what the color is FOR. Recommended standard tokens: `primary`, `secondary`, `accent`, `background`, `text` — use these when they fit. If the design has a specific non-standard purpose, write it freely: `\"CTA button\"`, `\"divider\"`, `\"error\"`, `\"Tiffany Blue\"`. **Don't describe the hue** (\"Warm Cream\", \"Near Black\") — the hex + swatch already show that; role is noise if it restates the appearance.\n- **fonts[]**: `family` (required, CSS font-family). `role` says what the font is FOR. Standard tokens: `heading`, `body`, `mono`. Or freeform: `\"display\"`, `\"code\"`, `\"caption\"`.\n- **logos[]**: `url` required. `role` says which variant / when to use: `primary`, `icon`, `wordmark`, or freeform like `\"dark-bg\"`, `\"watermark\"`. **⚠️ On update, logos REPLACE the entire array** — to add one, first call `manage_design_style` with `action: \"get\"`, then send the full new array.\n- **images[]**: `url` + `role` (required). `role` says what the image depicts / how to use it: `\"product hero\"`, `\"team photo\"`, `\"scene reference\"`, `\"moodboard\"`. Specific enough that downstream generators pick the right one per scene. **⚠️ Same REPLACE semantics as logos.**\n- **styleGuide**: high-level design tone only — one-line identity like \"Modern, technical, confident\" or \"Editorial, bold, high-contrast\". On update, replaces (empty string clears). Two hard rules:\n  - **Don't redescribe what other fields already show.** No \"warm off-white backgrounds with near-black text\", no \"gradient blue-pink-orange logo\", no \"Inter heading and body\". Those fields already carry that info.\n  - **Don't invent design prescriptions.** No layouts, animation styles, typography weights. Visual generators degrade when given prescriptive design briefs.\n  Only record abstract tone / voice / identity. When in doubt, keep it empty.\n\nOn update: colors/fonts merge by `role` (case-insensitive, whitespace-normalized) — patching `[{role:\"primary\", value:\"#new\"}]` updates only the primary, leaving others untouched. Logos/images/styleGuide replace.\n\nDesign style names are unique per user; server auto-appends \" (2)\", \" (3)\", etc. on collision — your call never fails on name conflict.","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","additionalProperties":false,"properties":{"action":{"description":"User library: list, get, create, update, apply, clear, delete. Catalog: list_presets, apply_preset. Supported actions: list, get, create, update, apply, clear, delete, list_presets, apply_preset.","type":"string"},"applyToProject":{"description":"For create / apply_preset: whether to auto-apply to the current project (default true).","type":"boolean"},"confirm":{"description":"For delete only: set to true ONLY after the user has confirmed the deletion. Omit on first call to trigger the confirmation_required protocol.","type":"boolean"},"confirmShared":{"description":"For update only: set to true after the user has confirmed it's OK to edit a design style used by multiple projects. Omit on first call so the confirmation_required response can surface the impact.","type":"boolean"},"designSpec":{"description":"For create: JSON string of a DesignSpec. The exact shape, field rules, and role conventions are in this tool's description under \"# designSpec shape\" — follow them verbatim. Core rule: every element carries one `role` label saying what it's FOR, not what it looks like.","type":"string"},"designStyleId":{"description":"For get / update (optional — defaults to the project's currently applied design style) or for apply / delete (required).","type":"string"},"locale":{"description":"For list_presets (optional): user's chat language code ('en', 'zh', 'zh-hant', 'es', or 'ja'). Catalog preset names/descriptions are stored with English canonical + locale overrides; passing the user's language returns localized text. Defaults to English if omitted.","type":"string"},"name":{"description":"For create (required): name for the new design style. Unique per user; server deduplicates with suffix if needed.","type":"string"},"patch":{"description":"For update: JSON string of a partial DesignSpec (same shape as designSpec — see \"# designSpec shape\"). Merge semantics per the description's field rules: colors/fonts merge by `role` (fonts fall back to `family`); ⚠️ logos / images / styleGuide REPLACE wholesale — to add one, `get` first, then send the full new array. Can be combined with `rename` in the same call.","type":"string"},"presetId":{"description":"For apply_preset (required): the catalog preset UUID returned by list_presets. The backend pins the project's designStyleId FK directly at this catalog row — no clone is made until the user actually edits the spec.","type":"string"},"projectId":{"description":"Existing project ID from the editor URL or list_projects. Pass this when continuing work on an existing project and the external MCP session is not already targeted.","minLength":1,"type":"string"},"rename":{"description":"For update: new name to rename the design style to. Can be combined with `patch` in the same call.","type":"string"},"scenario":{"description":"For list_presets (optional): scenario tag to narrow the catalog by. Catalogs tag themselves with applicableScenarios for recommendation / selection; omit to list all visible catalog presets.","type":"string"},"thumbnailUrl":{"description":"For create / update: optional UI cover image URL for the Design Style library / picker. This is not part of designSpec and is not used as a generation reference. Pass null on update to clear it.","type":["string","null"]}},"required":["action"],"type":"object"}}