{"name":"manage_markers","description":"Manage note markers on a timeline. Current contract: marker-note-v2. A marker is a point-or-range comment anchored to timeline frames — for example \"fix audio dip here\" or \"this cut is too tight\". Each marker has a frame position, optional duration, and one user-visible field: note.\n\nTwo marker scopes:\n- **Ruler markers** (default, scope:\"project\"): anchored at an absolute timeline frame. Stay where they are when clips move or get edited. Use for section labels, act breaks, timeline-level reminders.\n- **Clip markers** (scope:\"item\"): anchored to a specific clip via itemId. Travel with the clip across move, trim (either side), playback-rate change, split, and duplicate, staying glued to the same source moment. Use for clip-specific notes like \"color grade this shot\" or \"tighten the cut here\".\n\nUse this tool to:\n- list: show note markers on the active timeline (both scopes), or on timelineId when provided.\n- get: fetch one marker by prefix id.\n- create: drop a note marker. Pass itemId to make it a clip marker; omit to make it a ruler marker. Defaults: durationFrames=0 (point). fromFrame is always in timeline-frame coordinates — the tool converts internally when itemId is set.\n- batch_create / bulk_create: drop multiple note markers in one call (each entry can target a different itemId or none).\n- update: change an existing marker's fromFrame, durationFrames, and/or note.\n- batch_update / bulk_update: update multiple existing markers in one call.\n- delete: remove an existing marker by prefix id, or several at once via markerIds (batch_delete / bulk_delete are aliases) — one call for the whole cleanup, never one call per marker.\n- transcript-backed notes: for exact transcript quotes, pass transcriptSegments plus optional notePrefix instead of free-text note. The tool copies transcript text and fills fromFrame from the first selected segment unless you pass fromFrame explicitly.\n\nChoosing scope:\n- \"mark this clip\" / \"note on this shot\" / \"remember to fix this take\" → clip marker (set itemId from preview_timeline items).\n- \"section header at 0:30\" / \"act break\" / \"TODO at frame X\" → ruler marker (omit itemId).\n- When the user names or selects a specific clip, prefer clip marker.\n\nEvidence-bound placements: when the user asks for a marker at a transcript/caption phrase, visual moment, current selection, or other evidence-derived location, create only after you have exact frame/timestamp evidence from the user or a current tool result. If lookup fails, report that no marker was placed; do not use frame 0 or any placeholder fallback unless the user explicitly asked for start-of-timeline.\n\nTemporary/probe markers are still real project markers. If you create one for verification, delete it and list markers to prove cleanup before optional work or final answer.\n\nBy default markers are scoped to the active timeline. If the user names or identifies a non-active timeline, pass timelineId from manage_timelines list instead of switching active timelines. Results echo the target timeline and the resulting scope so you can verify placement without an extra list/read call. Only pass the schema fields for this tool: action, timelineId, fromFrame, itemId, optional durationFrames, markerId, markerIds, note, markers, updates, transcriptSegments, transcriptTrack, and notePrefix.","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","additionalProperties":false,"properties":{"action":{"description":"list, get, create, batch_create, bulk_create, update, batch_update, bulk_update, delete, batch_delete, or bulk_delete","type":"string"},"durationFrames":{"description":"Duration in frames (0 = point marker, default 0)","maximum":9007199254740991,"minimum":0,"type":"integer"},"fromFrame":{"description":"Integer timeline frame to anchor the marker at (required for create unless transcriptSegments is used). When itemId is set this is still the timeline frame — the tool converts internally so the marker stays glued to the clip's source content. For evidence-bound requests, do not use 0 as an unknown fallback.","maximum":9007199254740991,"minimum":0,"type":"integer"},"itemId":{"description":"Optional timeline item (clip) id or prefix. When set, creates/identifies a clip marker anchored to that clip instead of the timeline ruler. Use for notes that should travel with a clip across move/trim/split/duplicate. Omit for a ruler marker.","type":"string"},"markerId":{"description":"Target marker prefix id from action=list (required for get/update; delete takes this or markerIds)","type":"string"},"markerIds":{"description":"Several marker prefix ids to remove in one delete/batch_delete call. Use this instead of repeated single deletes.","items":{"type":"string"},"type":"array"},"markers":{"description":"Markers to create in one call (required for batch_create/bulk_create)","items":{"additionalProperties":false,"properties":{"durationFrames":{"description":"Duration in frames (0 = point marker, default 0)","maximum":9007199254740991,"minimum":0,"type":"integer"},"fromFrame":{"description":"Integer timeline frame to anchor the marker at (omit to use transcriptSegments placement). When itemId is set, this is still the timeline frame — the tool converts to source-relative storage so the marker rides the clip through trim/move/split/playback-rate changes. For evidence-bound requests, do not use 0 as an unknown fallback.","maximum":9007199254740991,"minimum":0,"type":"integer"},"itemId":{"description":"Optional timeline item (clip) id or prefix. When set, creates a clip marker anchored to that clip; the marker survives clip move/trim/split/duplicate. Omit for a timeline/ruler marker (the default).","type":"string"},"note":{"description":"Marker note text (omit when using transcriptSegments)","type":"string"},"notePrefix":{"description":"Optional label prefix when transcriptSegments derives the note body","type":"string"},"transcriptSegments":{"description":"Active Script segment ids/ranges from timeline.md, e.g. \"3-4\"; note text is copied from read_script output","type":"string"},"transcriptTrack":{"description":"Track filter for transcriptSegments, e.g. V1 or A1","type":"string"}},"type":"object"},"type":"array"},"note":{"description":"Marker note text (required for create unless transcriptSegments is used)","type":"string"},"notePrefix":{"description":"Optional label prefix when transcriptSegments derives the note body","type":"string"},"projectId":{"description":"Existing project ID from the editor URL or list_projects. Pass this when continuing work on an existing project and the external MCP session is not already targeted.","minLength":1,"type":"string"},"timelineId":{"description":"Timeline ID or prefix to target without switching active timelines. Use for non-active timeline marker work.","type":"string"},"transcriptSegments":{"description":"Active Script segment ids/ranges from timeline.md, e.g. \"3-4\"; note text is copied from read_script output","type":"string"},"transcriptTrack":{"description":"Track filter for transcriptSegments, e.g. V1 or A1","type":"string"},"updates":{"description":"Markers to update in one call (required for batch_update/bulk_update)","items":{"additionalProperties":false,"properties":{"durationFrames":{"description":"New duration in frames (0 = point marker)","maximum":9007199254740991,"minimum":0,"type":"integer"},"fromFrame":{"description":"New integer frame to anchor the marker at","maximum":9007199254740991,"minimum":0,"type":"integer"},"markerId":{"description":"Target marker prefix id from action=list","type":"string"},"note":{"description":"New marker note text","type":"string"},"notePrefix":{"description":"Optional label prefix when transcriptSegments derives the note body","type":"string"},"transcriptSegments":{"description":"Active Script segment ids/ranges from timeline.md, e.g. \"3-4\"; note text is copied from read_script output","type":"string"},"transcriptTrack":{"description":"Track filter for transcriptSegments, e.g. V1 or A1","type":"string"}},"required":["markerId"],"type":"object"},"type":"array"}},"required":["action"],"type":"object"}}