{"name":"convert_motion_graphic_to_video","description":"Convert a motion graphic into a real, transparent video asset (vp8/WebM alpha).\n\nStep 1 of the MG→video convert flow: kicks a cloud render of the MG at its native length and returns a durable renderId. Then:\n  1. Call track_export with the renderId until status is \"complete\" and read outputUrl.\n  2. Call register_converted_video with { mgAssetId, outputUrl } to import the render as a video asset in the media pool.\n\nOne MG maps to one deterministic video asset, so re-converting the same MG dedupes to the existing asset. Use this when the user wants an MG as a plain video — to overlay on footage, or to replace the MG clip with a real video clip (via edit_item after the video asset exists).","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","additionalProperties":false,"properties":{"assetId":{"description":"Motion-graphic asset id or prefix to convert.","type":"string"},"projectId":{"description":"Existing project ID from the editor URL or list_projects. Pass this when continuing work on an existing project and the external MCP session is not already targeted.","minLength":1,"type":"string"}},"required":["assetId"],"type":"object"}}