{"name":"apply_script","description":"Apply the edits already authored as `timeline.md` to the canonical timeline. This is the commit step for Script edits: it diffs the file against the live state and commits the core timeline change atomically (any core-row error rolls back the whole apply). After a successful apply, `timeline.md` is re-materialized clean (no `~~…~~`). Read it again only when starting another global semantic editing pass.\n\n# Workflow\n\nPair with `read_script`: first call `read_script`, then edit the materialized `timeline.md`, then call this tool — using the same transport `read_script` established: workspace surfaces edit `timeline.md` in place and call `apply_script()` with no `timelineMd`; external surfaces edit the inline `timelineMd` string and pass the full edited content back via `apply_script({timelineMd: \"\u003cedited\u003e\"})`. For preview before commit, pass `preview: true`.\n\n# Valid Script edits\n\nThe edit syntax — strike forms (`[s5] keep~~drop~~keep`), silence markers, the no-rewrite rule for `[sN]` rows, Chinese no-added-spaces — is defined in `read_script`; author edits exactly per that contract. Rules that matter at commit time:\n\n- Author the whole arrangement (reorders, repeats, multiple versions back-to-back) and commit it in one apply.\n- Occurrence anchors: moving an existing `[sN@M]` row preserves that occurrence; adding a bare `[sN]` row creates another play of that content. Preserve the `@N` suffix on the specific existing occurrence you edit, move, or delete; for hook replacement, remove only the copied hook occurrence rows, not the body occurrence.\n- Matching is tolerant to case, punctuation, and accidental word-boundary whitespace (`and it` vs `andit`); it is NOT tolerant to changing the spoken words themselves. Use `manage_transcript` action `fix` when the transcript text is wrong.\n\n# Expected timeline result\n\nAs documented in `read_script`: middle deletions/moves split clips around the kept ranges — more clips is expected physical editing, not a failure. Cover distracting jump cuts after the A-roll edit.\n\n# Output\n\nThe successful result is a bounded summary. Preview retains positional and removed-row detail for inspection.\n\n# Caveats\n\n- This does not correct ASR mistakes. If the source transcript text itself is wrong, use `manage_transcript` action `fix` before editing Script.\n- Do not simplify an existing `[sN@M]` row to `[sN]` while editing, moving, or deleting that occurrence; that loses identity and can target the wrong duplicate. Only a newly added replay row should be bare `[sN]`.\n- Each `[sN]` line must be a contiguous substring of its source segment. Do NOT rewrite or fabricate words.\n- Inline strikethrough is valid: `[s4] ~~uh, ~~improvements` deletes only `uh, ` and keeps `improvements`. Never expand a user-approved inline strike to a whole-line deletion.\n- If the timeline was modified externally since the last `read_script`, apply refuses (stale state). Recovery: re-materialize via `read_script`, review the new state, then re-edit and re-apply.\n- An exceptionally large commit is rejected before writing. Commit fewer Script edits, then call `read_script` again before continuing; use `track` to isolate one track when the file contains several.\n\nDesktop MCP: after `read_script`, either pass the complete modified `timelineMd` returned inline, or (when your CLI can edit workspace files) omit it so the desktop bridge reads the workspace `timeline.md`.","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","additionalProperties":false,"properties":{"preview":{"description":"Preview the result without modifying the timeline.","type":"boolean"},"projectId":{"description":"Existing project ID from the editor URL or list_projects. Pass this when continuing work on an existing project and the external MCP session is not already targeted.","minLength":1,"type":"string"},"timelineMd":{"description":"Full edited timeline.md content. Required when the surface has no workspace filesystem (read_script returned the timelineMd inline). Omit on surfaces where read_script materialized timeline.md to your workspace; ChatCut will read your in-place edits from disk.","type":"string"},"track":{"description":"Track alias (e.g. V1, A1) or track UUID. Usually unnecessary; the default speech track is used.","type":"string"}},"type":"object"}}