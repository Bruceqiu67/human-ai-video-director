{"name":"isolate_voice","description":"Apply or clear AI voice isolation on a video or audio item with spoken human voice. This is not generic denoise — it is speech-only enhancement.\n\nIMPORTANT — only for clips that contain spoken human voice:\nThe open editor selects its native speech-enhancement engine: ChatCut Desktop uses flux MossFormer2 with a persistent disk cache; Web uses DeepFilterNet3. Both suppress everything EXCEPT human speech. Applying voice isolation to a clip with no speech (music, ambient noise, sound effects, action footage) will distort or silence the audio. If the user's clip does not contain spoken voice, tell them ChatCut does not have a suitable tool for that request.\n\nDesktop MossFormer2 runs at its native fixed strength; Desktop normalizes legacy strength values to 100. Web DeepFilterNet3 continues to support strength 0-100.\n\nUse this tool for spoken-voice dereverb too:\nWhen the user asks to reduce room echo, reverb, reverberation, or \"echoey\" / \"distant\" speech (去混响 / 回声大 / 空旷感 / 人声不贴耳), treat it as a voice-isolation use case if the target clip contains spoken human voice. Do not use it as generic dereverb for music, ambience, SFX, or non-speech recordings.\n\nHow this tool works:\n- Preferred route when the editor is open: call action=\"apply\" with itemId and sourceAssetId, no filePath. The open editor runs the same platform-native AI Voice Isolation pipeline as the manual UI, creates the derived audio asset, and points the item at it.\n- Workspace route: run the `deep-filter` binary (DeepFilterNet3) locally to produce a voice-isolated WAV, then call action=\"apply\" with filePath and sourceAssetId. The local engine MUST be DeepFilterNet3, or the derivative is incompatible with the editor's cache.\n- External connector route: call import_media action=\"create_session\", run the asset-import upload-media.mjs helper on the already-cleaned WAV, then action=\"attach\" with the returned audio assetId. The source asset is never modified; clearing reverts to the original.\n\nIf DeepFilterNet3 is missing, install it; do not substitute generic ffmpeg denoise. Use a `deep-filter` release binary, or a temp Python 3.11 venv with `deepfilternet==0.5.6`, `torch==2.3.1`, `torchaudio==2.3.1`, `soundfile==0.12.1`; run `deep-filter-py --model-base-dir DeepFilterNet3 --atten-lim \u003c0-100\u003e --output-dir \u003cdir\u003e \u003cinput.wav\u003e`.\n\nWorkspace workflow (driven by the `voice-isolation` skill when the frontend route is unavailable):\n1. Use preview_timeline to find the target item, then inspect_item for its assetId and audio state. If it shows no speech signals and likely has no spoken voice, do NOT proceed — tell the user.\n2. Get the full source asset audio locally, not just the visible timeline range; timeline items can have sourceStart offsets. For video, extract the full audio track with ffmpeg and transcode to mono 48 kHz WAV.\n3. Run DeepFilterNet3 on that full-source WAV: `deep-filter -a \u003c0-100\u003e \u003cinput.wav\u003e` or `deep-filter-py --model-base-dir DeepFilterNet3 --atten-lim \u003c0-100\u003e --output-dir \u003cdir\u003e \u003cinput.wav\u003e` (atten-lim-db: 0=no reduction, 100=full).\n4. apply (workspace filePath) or attach (already-uploaded denoisedAssetId).\n\nitemId / sourceAssetId / denoisedAssetId accept 10-hex prefix ids or full UUIDs.\n\nActions:\n- apply: itemId, sourceAssetId (the item's current assetId), optional strength (0-100, default 100). With no filePath, asks the open editor to run AI Voice Isolation. With filePath, uploads the workspace wav and points the item at it; strength must match the deep-filter -a value.\n- attach: itemId, denoisedAssetId (an already-uploaded voice-isolated audio asset), sourceAssetId, optional strength. Points the item at that asset. On connectors with no workspace, import the cleaned WAV through import_media plus the asset-import upload-media.mjs helper, then attach its returned assetId.\n- clear: itemId only. Reverts to the original audio.\n\nReturns JSON describing what changed.\n\nExternal MCP voice-isolation rule: prefer action=\"apply\" without filePath when the project is open in the editor; ChatCut routes the request through the editor tool bridge so the browser runs the native AI Voice Isolation pipeline. If no editor is open, create a DeepFilterNet3 full-source WAV locally, call import_media action=\"create_session\", run the asset-import upload-media.mjs helper on that WAV, then call action=\"attach\" with the returned assetId as denoisedAssetId plus sourceAssetId and strength. Do not pass workspace filePath on this connector.","parameters":{"$schema":"https://json-schema.org/draft/2020-12/schema","additionalProperties":false,"properties":{"action":{"enum":["apply","attach","clear"],"type":"string"},"denoisedAssetId":{"description":"attach only: assetId of the already-uploaded voice-isolated audio asset.","type":"string"},"itemId":{"description":"Target audio/video item id (prefix or full UUID) from preview_timeline.","type":"string"},"projectId":{"description":"Existing project ID from the editor URL or list_projects. Pass this when continuing work on an existing project and the external MCP session is not already targeted.","minLength":1,"type":"string"},"sourceAssetId":{"description":"apply/attach: the item's current assetId (the source used for voice isolation).","type":"string"},"strength":{"description":"apply/attach: voice isolation strength 0-100 (atten-lim-db), default 100.","maximum":100,"minimum":0,"type":"number"}},"required":["action","itemId"],"type":"object"}}